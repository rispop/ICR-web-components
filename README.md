## Building the Paper and Slides
The LaTeX source code to build a paper and slide presentation explaining this research are located in the root directory of this repository

To build the PDF paper, inside the root of this repository, run
```
pdflatex research-paper.tex
```

To build the PDF presentation, inside the root of this repository, run
```
pdflatex presentation.tex
```
